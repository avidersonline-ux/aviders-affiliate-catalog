# Aviders Affiliate Catalog (static JSON)
Serves chunked product JSON via GitHub Pages. Deployed by Actions.
Base URL after deploy:
https://<your-username>.github.io/aviders-affiliate-catalog
Data lives in /data/
